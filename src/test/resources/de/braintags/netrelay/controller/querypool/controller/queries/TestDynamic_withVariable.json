{
  "description": "This is a test query",
  "mapper": "Person",
  "operation": "select",
  "dynamic": {
    "orderBy": "score asc",
    "query": {
      "condition": {
        "field": "firstname",
        "logic": "=",
        "value": "${request.param}"
      }
    }
  }
}