{
  "description": "Test query that has a variable as orderBy clause",
  "mapper": "Person",
  "operation": "select",
  "orderBy": "${request:sort} ${request:dir}",
  "dynamic": {
    "query": {
      "condition": {
        "field": "firstname",
        "logic": "=",
        "value": "max"
      }
    }
  }
}