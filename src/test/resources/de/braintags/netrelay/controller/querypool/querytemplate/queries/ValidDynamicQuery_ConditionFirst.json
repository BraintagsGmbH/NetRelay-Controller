{
  "description": "This is a valid dynamic query",
  "mapper": "Person",
  "operation": "select",
  "dynamic": {
    "orderBy": "${request.params.order}",
    "query": {
 	   "condition": {
     	  "field": "firstname",
          "logic": "=",
          "value": "max"
	   }
    }
  }
}